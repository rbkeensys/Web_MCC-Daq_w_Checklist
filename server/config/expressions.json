{
  "expressions": [
    {
      "name": "Spark",
      "enabled": true,
      "expression": "count    = static.sparkCount\nbutton   = buttonVars.sparkON\nselfBuzz = buttonVars.selfBuzz\narm      = buttonVars.armIgnition\nsparkon  = 0\n\nbutton   = button or static.ignition\n\nif arm == 1 then\n if selfBuzz == 1 and button == 1 then\n    \"DO:Spark\" = 1\n endif\n\n if selfBuzz == 0 and button == 1 and arm == 1 then\n    sparkon = 1\n    if \"DO:Spark\" == 1 then\n       \"DO:Spark\" = 0\n    else\n       \"DO:Spark\" = 1\n    endif\n endif\nelse\n \"DO:Spark\" = 0\nendif\n\nif button == 0 then\n   \"DO:Spark\" = 0\nendif    \n\ncount = static.sparkCount\nspark = \"DO:Spark\"\n\"DO:Igniter\" = sparkon\nsparkon\n",
      "execution_rate_hz": null
    },
    {
      "name": "PressureSet",
      "enabled": true,
      "expression": "// Desired mini-tank pressure \nstatic.pressureSetPoint = 50\nstatic.fuelDelta = 0\n",
      "execution_rate_hz": null
    },
    {
      "name": "LOXFuelOutletControl",
      "enabled": true,
      "expression": "// Controls individually and together the LOX and Fuel outlet valves\n\nrunState     = static.testState\nLOXOut       = buttonVars.LOXOutEn \nFuelOut      = buttonVars.FuelOutEn\ntimedFuel    = buttonVars.TimedFuelOut or buttonVars.mixedOut\ntimedLOX     = buttonVars.TimedLOXOut or buttonVars.mixedOut\nLOXbyWeight  = static.LOXOutValve\nFuelbyWeight = static.FuelOutValve\n\ngoLOX  = 0\ngoFuel = 0\n\nif (FuelbyWeight or LOXOut or LOXbyWeight) then\n        goLOX = 1\nendif\nif (FuelOut or FuelbyWeight) then\n        goFuel = 1\nendif\n        \n\"DO:LOXOUT\"  = goLOX\n\"DO:FuelOUT\" = goFuel\n\n",
      "execution_rate_hz": null
    },
    {
      "name": "Test Sequencer",
      "enabled": true,
      "expression": "// Test Sequencer\n// Uses a state machine to step through \n// shut off mixing, purge, mix on, pause, ignite\n// run a set time, mix off, purge - done\n\nstatic.testState      //idle\n\nstatic.deltaTime     \nstatic.preStartTime    = 500\nstatic.prePurgeTime    = 1500\nstatic.prePurgeOverlap = 0\nstatic.ignitionTime    = 600\nstatic.postVentTime    = 2000\nstatic.postBleedTime   = 2000\nstatic.postPurgeTime   = 4000\nstatic.abort1PurgeTime = 1000\nstatic.abort2PurgeTime = 4000\nstatic.pressTimeOut    = 2000\nstatic.runTime         = 3000\nstatic.overlapRun\n\nstatic.ignition      // these output to control expressions that take in buttons or sequence output\nstatic.LOXVent  \nstatic.FuelVent \nstatic.Purge    \nstatic.LOXOut   \nstatic.FuelOut  \n\nstartTest = buttonVars.runTest\nabort     = buttonVars.abort or buttonVars.purge\npurgeIn   = buttonVars.purge\n\nTankPress = \"AI:Ch P\"\nsetP      = static.runTankSetPoint\n\nIDLE           = 0\nPRESTART       = 1  // condition on, press off\nPREPURGE       = 2  // purge on, wait timed amount\nSTARTPRESS     = 3  // purge off, press on wait for press >= set point\nIGNITION       = 4  // fire \nRUN            = 5  // wait run time, goes to POSTPURGE\nPOSTPURGE      = 6  // purge, goes through engine\nABORTPOSTVENT  = 7  // vent only, depress tank\nABORTPOSTBLEED = 8  // purge + bleed, clear line\nABORTPOSTPURGE = 9  // purge, goes through engine\nABORT          = 10  // go through the abort post events\n\nstate     = static.testState\ndeltaT    = static.deltaTime\n\npurge   = 0\nvent    = 0\nbleed   = 0\nigniton = 0\n\nif ( (abort or buttonVars.purge) and (state) and (state < ABORT) ) then\n    static.deltaTime = 0\n    static.overlapRun = 0\n    state = ABORT\nendif\n\nif ( state == IDLE ) then    // IDLE\n    if ( startTest > 0 ) then\n        static.deltaTime = 0\n        static.overlapRun = 0\n        state=PRESTART\n    endif\nelse if ( state == PRESTART ) then\n    mixon = 0\n    if ( deltaT > static.preStartTime ) then\n        static.deltaTime = 0\n        state = PREPURGE\n    endif\nelse if ( state == PREPURGE ) then\n    purge = 1\n    if ( deltaT > static.prePurgeTime ) then\n        state = STARTPRESS\n        static.deltaTime = 0\n        purge = 0\n    endif\nelse if ( state == STARTPRESS) then\n    if ( TankPress >= setP ) then\n        state = IGNITION\n        static.deltaTime = 0\n    endif\n    if ( deltaT > static.pressTimeOut ) then\n        state = ABORT\n        static.deltaTime = 0\n    endif\nelse if ( state == IGNITION ) then\n    ignition = 1\n    if ( deltaT > static.ignitionTime ) then\n        static.deltaTime = 0\n        state = RUN\n    endif\nelse if ( state == RUN ) then\n    if ( deltaT > static.runTime ) then\n        static.deltaTime = 0\n        state = POSTPURGE\n    endif\nelse if ( state == POSTPURGE ) then\n    vent  = 0\n    purge = 1\n    bleed = 0\n    if ( deltaT > static.postPurgeTime ) then\n        static.deltaTime = 0\n        state = IDLE\n    endif\nelse if ( state == ABORTPOSTVENT ) then\n    vent  = 1\n    bleed = 0\n    purge = 0\n    if ( deltaT > static.postVentTime ) then\n        static.deltaTime = 0\n        state = ABORTPOSTBLEED\n    endif\nelse if ( state == ABORTPOSTBLEED ) then\n    vent  = 0\n    purge = 1\n    bleed = 1\n    if ( deltaT > static.postBleedTime ) then\n        static.deltaTime = 0\n        state = ABORTPOSTPURGE\n    endif\nelse if ( state == ABORTPOSTPURGE ) then\n    vent  = 1\n    purge = 1\n    bleed = 0\n    if ( deltaT > static.postPurgeTime ) then\n        static.deltaTime = 0\n        state = IDLE\n    endif\nelse if ( state == ABORT ) then\n    static.deltaTime = 0\n    state = ABORTPOSTVENT\nendif\n\nif ( state > 0 ) then\n    static.deltaTime = static.deltaTime + 7.215  //Valid for 160Hz update rate\nendif\n\nstatic.ignition    = ignition\nstatic.Purge       = purge\nstatic.RunTankVent = vent\nstatic.doBleed     = bleed\n\nstatic.testState = state\n\n\n",
      "execution_rate_hz": null
    },
    {
      "name": "Purge Control",
      "enabled": true,
      "expression": "// Manual and Auto Purge Valve Controller\n\nmanPurge     = buttonVars.purge\nautoPurge    = static.Purge\npressRunTank = static.pressRunTank\nstate        = static.testState\n\nif ((manPurge or autoPurge) or pressRunTank) then\n    purge = 1\n    \"DO:PURGE\" = purge\nelse\n    purge = 0\n    \"DO:PURGE\" = purge\nendif\n",
      "execution_rate_hz": null
    },
    {
      "name": "sparkRef",
      "enabled": true,
      "expression": "// Write your expression here\nsparkRef1 = static.ignition\nsparkRef2 = buttonVars.sparkON\nsparkRef = sparkRef1 or sparkRef2",
      "execution_rate_hz": null
    },
    {
      "name": "Line Condition LOX",
      "enabled": true,
      "expression": "// Line Conditioning for LOX\n// Conditioning valve on\n\nLOXBleed  = buttonVars.LOXBleedEn\n\nif ( LOXBleed ) then\n    static.LOXdoBleed = 1\nelse\n    static.LOXdoBleed = 0\nendif\n\n\"DO:LOXVent\" = static.LOXdoBleed\n\n",
      "execution_rate_hz": null
    },
    {
      "name": "RunTankPressurization",
      "enabled": true,
      "expression": "// Run Tank Press Controller\nstatic.runTankSetPoint = 50.0\nstatic.pressRunTank\nstatic.runTankPressLockout\n\ndepress     = buttonVars.depressRunTank\nautoDepress = static.RunTankVent\nmanualVent  = depress\nstate       = static.testState\nTankPress   = \"AI:Ch P\"\nsetP        = static.runTankSetPoint\nlockOut     = static.runPressTankLockout\nRunTankMaxP = static.RunTankMaxP\n\nif state >= 3 and state < 6 then  // PURGE complete, going to PRESSURIZE\n    enable = 1\nelse\n    enable = buttonVars.RunTankPressEn \nendif\n\nif depress or lockOut then\n    enable = 0\n    lockOut = 1\n    static.runPressTankLockout = lockOut\nendif\n\nif depress == 0 and buttonVars.RunTankPressEn == 0 then\n    lockOut = 0\n    static.runPressTankLockout = lockOut\nendif\n\nRun_Press_ON = 0\nRun_Vent_ON  = 0\n\nif (enable  and (TankPress < setP)) then\n    Tank_Press_ON = 1\n    Tank_Vent_ON  = 0\n    \"DO:PURGE\"    = Tank_Press_ON\nendif\n\nif ( depress ) then\n    Tank_Press_ON = 0\n    Tank_Vent_ON  = 1\n    \"DO:PURGE\"    = Tank_Press_ON\nendif\n\nif (TankPress > RunTankMaxP) then\n    Tank_Press_ON    = 0\n    Tank_Vent_ON     = 1\n    \"DO:RunTankVent\" = Tank_Vent_ON\nendif\n\nstatic.pressRunTank = Tank_Press_ON\n\n\"DO:RunTankVent\" = Tank_Vent_ON or autoDepress\nif Tank_Press_ON then\n    \"DO:PURGE\"       = Tank_Press_ON\n    static.FueldoBleed = 0 //line condition valve OFF\n    static.LOXdoBleed  = 0\nendif\n",
      "execution_rate_hz": null
    },
    {
      "name": "Line Condition Fuel",
      "enabled": true,
      "expression": "// Line Conditioning for Fuel\n// Conditioning valve on\n\nFuelBleed  = buttonVars.FuelBleedEn\n\nif ( FuelBleed ) then\n    static.FueldoBleed = 1\nelse\n    static.FueldoBleed = 0\nendif\n\n\"DO:FuelVent\" = static.FueldoBleed\n\n",
      "execution_rate_hz": null
    },
    {
      "name": "Press Valve Control",
      "enabled": true,
      "expression": "// LOX and Fuel Press Valve Control\n\nLOXAuto  = static.LOXPress\nFuelAuto = static.FuelPress\nsetP     = static.pressureSetPoint\n\nif LOXAuto != 0 then\n    \"DO:LOX Press\" = 1\nelse\n    \"DO:LOX Press\" = 0\nendif\n\nif FuelAuto != 0 then\n    \"DO:Fuel Press\" = 1\nelse\n    \"DO:Fuel Press\" = 0\nendif\n\n",
      "execution_rate_hz": null
    },
    {
      "name": "RunTankMaxP",
      "enabled": true,
      "expression": "// Run tank maximum pressure\nstatic.RunTankMaxP = 55",
      "execution_rate_hz": null
    },
    {
      "name": "LOX-Fill_by_Weight",
      "enabled": true,
      "expression": "// Load LOX by Weight\n\nstatic.LOXTargetWeight\nstatic.currentScale\nstatic.prevFillState\nstatic.tareWeight\nstatic.LOXOutValve\n\nscaleWeight = \"Scale:Scale0\"\nbutton      = buttonVars.FillLOXByWeight\nfillState   = static.prevFillState\n\nLOXTarget = static.LOXTargetWeight\nif LOXTarget == 0 then\n    LOXTarget = 200\n    static.LOXTargetWeight = LOXTarget\nendif\n\nif ( button ) then\n    if scaleWeight < LOXTarget then\n        LOXOut = 1\n        LOXWeight = scaleWeight\n        static.LOXWeight = LOXWeight\n    else\n        LOXOut = 0\n    endif\nelse\n    LOXOut = 0\nendif\n\nstatic.LOXOutValve = LOXOut\n\n        \n",
      "execution_rate_hz": null
    },
    {
      "name": "Fuel-Fill_By_Weight",
      "enabled": true,
      "expression": "// Fill Fuel by weight.\n// Read _weight continuously at first, when _weight rises by nn grams assume fuel is now going in.\n// Save the _weight at this point as _static.LOXWeight, calculate needed fuel _weight to reach _static.massRatio.\n// Continue to fill until total _weight equals target = LOXWeight * (1+MR)\n\nstatic.massRatio\nstatic.fuelFillState\nstatic.fuelWeight\n\nweight = \"Scale:Scale0\"\nbutton = buttonVars.FillFuelByWeight\nreset  = buttonVars.resetFuelFill\n\ntargetWeight = static.targetWeight\ntargetFuel   = static.targetFuel\nLOXWeight    = static.LOXWeight\nfuelWeight   = static.fuelWeight\nfullWeight   = static.fullWeight\nfuelOut      = static.FuelOutValve\nminWeight    = static.minWeight\nstate        = static.fuelFillState\nmassRatio    = static.massRatio\n\nIDLE             = 0\nSTART            = 1\nWAITFORMASSUP    = 2\nCALCNEEDEDMASS   = 3\nWAITFORFINALMASS = 4\nCOMPLETE         = 5\n\nif ( reset ) then\n    state = IDLE\nendif\n\nif ( state == IDLE ) then\n    if ( button ) then\n        state = START\n    endif\n    fuelOut = 0\nelse if ( state == START ) then\n    minWeight = weight\n    state = WAITFORMASSUP\nelse if ( state == WAITFORMASSUP ) then\n    fuelOut = 1\n    if ( weight <= minWeight) then\n        minWeight = weight\n    else if ( weight > minWeight + 1 ) then\n        LOXWeight = weight\n        state = CALCNEEDEDMASS\n    endif\nelse if ( state == CALCNEEDEDMASS ) then\n    fuelOut = 0\n    targetFuel   = LOXWeight * massRatio\n    targetWeight = LOXWeight + targetFuel\n    state = WAITFORFINALMASS\nelse if ( state == WAITFORFINALMASS ) then\n    fuelWeight = weight - LOXWeight\n    if ( weight > targetWeight ) then\n        fuelOut = 0\n        state = COMPLETE\n    else\n        fuelOut = 1\n    endif\nelse if ( state == COMPLETE ) then\n    state = IDLE\nendif\n\n\nstatic.fuelFillState = state\nstatic.FuelOutValve = fuelOut\n\nfullWeight          = weight\nstatic.minWeight    = minWeight\nstatic.LOXWeight    = LOXWeight\nstatic.targetFuel   = targetFuel\nstatic.targetWeight = targetWeight\nstatic.fuelWeight   = fuelWeight\nstatic.fullWeight   = fullWeight\n\n\n",
      "execution_rate_hz": null
    },
    {
      "name": "massRatio",
      "enabled": true,
      "expression": "// Fuel to LOX ratio\nstatic.massRatio = 0.394\n",
      "execution_rate_hz": null
    }
  ]
}